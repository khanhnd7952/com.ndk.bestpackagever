using UnityEngine;

namespace lib.ndk
{
    public class NewTestScripts : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("Hello World!");
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
